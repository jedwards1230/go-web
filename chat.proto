syntax = "proto3";
package proto;
option go_package = "/proto";

message Request {
    string name = 1;
}

message Response {
    string greeting = 1;
}

service ChatService {
    rpc Hello(Request) returns (Response) {};
}